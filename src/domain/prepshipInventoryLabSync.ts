/**
 * KULLANILMIYOR — bu dosya bir dosya adı değişikliği sırasında oluştu
 * ancak eski dosyayı silme izni olmadığı için kanonik içerik
 * `src/domain/prepshipReconciliation.ts` içinde kaldı (bkz. o dosyanın
 * başındaki not — denetim raporu §17). Kod bu dosyayı hiçbir yerden
 * import etmiyor; kafa karışıklığını önlemek için burada bırakıldı.
 */
export {};
